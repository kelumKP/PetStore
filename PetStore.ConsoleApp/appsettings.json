{
  // Region: ApiSettings

  "ApiSettings": {
    // Base URL of the Pet Store API
    "BaseApiUrl": "https://petstore.swagger.io/v2",

    // API endpoint for finding pets by status
    "ApiEndpoint": "pet/findByStatus",

    // The status of pets to search for (e.g., "available")
    "Status": "available"
  }

  // End of Region: ApiSettings
}